/*write a program to implement linear search in any given array*/
#include <iostream>
using namespace std;
int main()
{
    int array[10];
    int min = 0;
    for (int i = 0; i < 10; i++)
    {
        int n;
        cout << "enter the value of index [" << i << "] = ";
        cin >> n;
        array[i] = n;
    }
    for(int i=0;i<10;i++){
        if(min<arr)
    }
}